syntax = "proto3";

package myCompany.mooc;  // Means Course is defined in this package

import "lecture.proto";

message Course {
  string name = 1;
  repeated string authors = 2;
  map<string, Lecture> lectures = 3;
}

